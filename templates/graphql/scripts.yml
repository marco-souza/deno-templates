scripts:
  start:
    desc: Start server
    cmd: ./mod.ts
    imap: ./imports.json
    allow:
      - env
      - net

  watch:
    desc: Start server
    cmd: ./mod.ts
    imap: ./imports.json
    watch: true
    allow:
      - env
      - net
